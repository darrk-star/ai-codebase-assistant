{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Streamlit UI",
      "type": "node-terminal",
      "request": "launch",
      "command": "${workspaceFolder}\\.venv\\Scripts\\python.exe -m streamlit run app/ui.py --server.port 8502",
      "cwd": "${workspaceFolder}"
    }
  ]
}
